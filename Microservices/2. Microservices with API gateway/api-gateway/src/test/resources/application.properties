# Run standalone in tests: no Eureka, and disable the discovery-based route
# locator (it needs a live registry). The LogFilter is still verified.
eureka.client.enabled=false
spring.cloud.gateway.discovery.locator.enabled=false
