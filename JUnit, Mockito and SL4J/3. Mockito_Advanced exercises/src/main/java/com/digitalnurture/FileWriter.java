package com.digitalnurture;

/**
 * Exercise 3: file-writer abstraction to be mocked.
 * (A custom interface, not java.io.FileWriter.)
 */
public interface FileWriter {
    void write(String content);
}
